{
  "name": "workar-server",
  "version": "0.1.3",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "https://github.com/damiant/workar.git",
    "directory": "server-cli"
  },
  "bin": {
    "workar-server": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/cli.js",
    "prepack": "mkdir -p image-gen-cli && cp ../image-gen-cli/src/*.js image-gen-cli/"
  },
  "files": [
    "dist/",
    "image-gen-cli/",
    "work-defs.json",
    "README.md"
  ],
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
