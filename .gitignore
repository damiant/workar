# ── Dependencies ──────────────────────────────────────────────────────────────
node_modules/
.pnp/
.pnp.js

# ── Build output ──────────────────────────────────────────────────────────────
dist/
build/
.cache/
.wrangler/

# ── Test artefacts ────────────────────────────────────────────────────────────
worker/.test-temp.db
worker/.test-temp.db-shm
worker/.test-temp.db-wal
coverage/

# ── CLI caches & generated output ─────────────────────────────────────────────
.img-cli/
.tts-cli/
.gemma4-cli/
*.wav

# ── Server prepack output (generated by npm run prepack before publish) ──────
server-cli/image-gen-cli/
server-cli/tts-cli/
server-cli/gemma4-cli/

# ── Secrets & env files ───────────────────────────────────────────────────────
.env
.env.*
!.env.example
# Wrangler local secrets (keep the .example)
.dev.vars
!.dev.vars.example

# ── Cloudflare / Wrangler ─────────────────────────────────────────────────────
.wrangler/

# ── tarsk client config lives in ~/.tarsk-work/ (outside repo) ───────────────
# Nothing to ignore here; listed as a reminder.

# ── img-cli binary & model weights (multi-GB) ────────────────────────────────
image-gen-cli/.img-c

# ── gemma4-cli binary & model weights (multi-GB) ─────────────────────────────
gemma4-cli/.gemma4-cli/
server-cli/.gemma4-cli/
server-cli/.img-cli/

# ── Generated images ──────────────────────────────────────────────────────────
*.png
*.jpg
*.jpeg
*.webp

# ── Logs ──────────────────────────────────────────────────────────────────────
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ── OS / Editor ───────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
*.swp
*.swo
*~

.img-cli
